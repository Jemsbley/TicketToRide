import american_board as a

class Controller:

    def __init__(self):
        # TODO field for the response modal through which we forward requests
        # TODO figure out how i want to interface this cuz it may be better to make this a parent and extend???
        return

    def draw_tickets(self, drawn: list[a.Ticket], min_keep: int) -> list[a.Ticket]:
        # TODO receive the full game state and actually determine which tickets to keep
        return []


